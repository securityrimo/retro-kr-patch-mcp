# 프로젝트 기록 시행 규약

초기 조사, PoC, 그래픽 카탈로그, HITL 관측과 QA 이슈는 증거·판정·다음 행동을 구분해 기록한다. 기존 프로젝트에 동등한 기록 체계가 있으면 그대로 유지한다. 아래 필드명과 상태명은 선택 프로필이며, 경로·직렬화 형식·도구는 고정하지 않는다.

번역 원문·역문·제어코드·검토 상태는 `references/conventions/translation-artifacts.md`에 따라 보존하고, 저장소 전체 레이아웃과 원본·파생 자산 취급은 `references/conventions/project-conventions.md`를 따른다.

## 목차

1. 공통 기록 규칙
2. 기록 배치
3. 초기 조사 기록
4. PoC 판정 기록
5. 베이크드 텍스트 카탈로그
6. HITL 관측 요청
7. QA 라운드와 이슈
8. 기록 검증

## 1. 공통 기록 규칙

프로젝트 기록은 형식과 무관하게 다음 의미를 구분한다.

- **범위와 기준선**: 어느 원본·빌드·화면·자산·코드 경로에 대한 기록인지 식별한다.
- **증거 귀속**: 실행·관측 증거를 그것을 만든 정확한 입력·산출물·환경에 연결한다. 바뀔 수 있는 경로나 상태 이름만으로 내용 동일성을 대신하지 않는다.
- **관찰과 해석**: 직접 관찰한 사실, 아직 검증되지 않은 가설, 격리로 확정한 결론을 섞지 않는다.
- **근거**: 해시, 덤프, 스크린샷, 트레이스, 디스어셈블 결과, 재현 절차처럼 판정을 다시 확인할 수 있는 포인터를 둔다.
- **판정**: 통과, 실패, 불명확을 구분하고 판정 기준과 실제 결과를 함께 남긴다.
- **다음 행동**: 실패하면 버릴 가정, 불명확하면 수행할 더 작은 확인, 통과하면 승격할 지식이나 후속 단계를 연결한다.

컬렉션의 항목은 재정렬·파일 이동 뒤에도 유지되는 안정 ID를 쓴다. 주소·오프셋·파일명만 ID로 쓰면 재빌드나 재배치 때 추적이 끊기므로, 별도 논리 ID와 현재 물리 위치를 분리한다. 같은 사실을 여러 기록에 복사하지 않고 해당 항목의 ID나 프로젝트 상대 경로를 참조한다.

## 2. 기록 배치

기존 저장소에서 같은 책임을 맡는 기록 위치와 연결 방식을 우선한다. 새로운 위치가 필요해도 이 문서는 폴더명이나 파일명을 정하지 않는다. 대신 다음 책임을 유지한다.

- 한 종류의 현행 기록은 한 곳만 소유하고, 진입 문서는 그 위치를 한 번만 가리킨다.
- 현재 결론·활성 판정과 완료·폐기된 계획을 같은 상태로 섞지 않는다.
- 기록을 나누거나 합쳐도 안정 ID와 참조 관계가 유지되어야 한다.
- 대용량·저작물·환경 종속 증거를 저장소 밖에 두면 커밋되는 기록에 식별자와 재생성·재현 조건을 남긴다.

직렬화 형식과 배치는 기존 구조, 검토 방식, 기계 판독 필요성에 따라 프로젝트가 정한다. 형식 변경만을 위해 동등한 기존 기록을 이관하지 않는다.

## 3. 초기 조사 기록

### 3.1 아키텍처 기록

현재 검증된 구조만 본문으로 유지하고, 날짜순 실험 일지와 분리한다. 다음 표는 필요한 의미와 선택 가능한 필드명을 보여 준다.

| 필드 | 내용 |
|---|---|
| `scope` | 대상 원본 리비전과 조사한 매체·파일·코드 경로 |
| `confirmed` | 실측이나 실행 추적으로 확정한 구조와 근거 포인터 |
| `hypotheses` | 남은 가설, 판정에 필요한 관측, 우선순위 |
| `memory_map` | 확인된 주소 공간·뱅크·파일 오프셋 변환 |
| `control_flow` | 필요한 범위의 호출·디스패치·데이터 흐름 |
| `unknowns` | 후속 설계를 막는 미지수와 가장 싼 다음 확인 |

단일 렌더 경로만 확인하면 되는 프로젝트는 전체 호출 그래프를 만들 필요가 없다. 그래프가 필요하면 노드와 엣지의 의미, 가설/확정 상태, 근거 위치를 함께 기록한다.

### 3.2 텍스트 맵

대표 표본부터 기계 판독 가능하게 기록하고, 본 추출 단계에서 전량으로 확장한다. 다음 의미를 구분한다.

| 필드 | 내용 |
|---|---|
| `asset_id` | 텍스트 영역이나 테이블의 안정 ID |
| `source` | 컨테이너·파일·오프셋·크기와 원본 해시 |
| `boundaries` | 엔트리 경계, 정렬, 종료자, 패딩 |
| `pointers` | 포인터 테이블 위치, 폭, 기준 주소, 중복·널 의미 |
| `encoding` | 문자 대응과 확정 근거 |
| `controls` | 제어코드, 인자 폭, 상태 영향, 가설/확정 구분 |
| `sample` | 원시 바이트와 가역 디코딩 표본 |
| `roundtrip` | 재조립 결과와 바이트 동일성 판정 |

### 3.3 실현 가능성 평가

위험 항목마다 `risk_id`, 범위, 관찰 근거, 영향, 가능한 우회안, 현재 판정, 다음 확인을 둔다. 글리프·인코딩·저장 공간·활성 메모리·훅·압축·런타임 자산 경계를 정량화할 수 있으면 측정값과 상한을 함께 기록한다. 최종에는 go/no-go 또는 조건부 진행 판정과 그 판정을 뒤집을 수 있는 남은 미지수를 명시한다.

## 4. PoC 판정 기록

각 PoC 게이트의 판정은 다른 게이트와 섞이지 않는 하나의 기록 항목으로 남긴다. 파일을 나눌지는 기존 기록 구조가 정한다. 선택 필드 프로필은 다음과 같다.

| 필드 | 내용 |
|---|---|
| `gate_id` | 전략 문서의 게이트와 연결되는 안정 ID |
| `trigger` | 이 게이트를 켠 위험 신호와 되돌리기 비용 |
| `hypothesis` | 실험이 구분하려는 가정 |
| `target` | 대표 화면·텍스트·글리프·코드 경로와 선택 근거 |
| `criteria` | 통과·실패·불명확 기준과 각 결과의 다음 행동 |
| `procedure` | 재현 가능한 최소 조작과 기준 빌드 |
| `result` | 실제 관찰, 증거 포인터, 판정 |
| `proved` | 통과 시 본 구현으로 승격할 지식 |
| `not_proved` | 이 실험으로는 일반화할 수 없는 범위 |
| `discarded` | 기각한 가정과 본 구현에 넣지 않을 임시 산출물 |
| `next` | 이어서 수행하거나 생략할 게이트와 이유 |

수동 헥스 편집, 임시 오프셋, 단발 스크립트는 결과 근거로 보존할 수 있지만 반복 빌드 입력으로 표시하지 않는다.

## 5. 베이크드 텍스트 카탈로그

등재 단위는 텍스처 파일이 아니라 텍스트 블록이다. 한 텍스처에 라벨이 여러 개면 각 라벨을 별도 항목으로 둔다. 다음 표는 필요한 의미와 선택 가능한 필드명을 보여 준다.

| 필드 | 내용 |
|---|---|
| `block_id` | 텍스트 블록의 안정 ID |
| `source` | 컨테이너 경로·인덱스와 원본 해시 |
| `bounds` | 픽셀 단위로 실측한 `x`, `y`, `w`, `h` |
| `format` | 픽셀 포맷, 팔레트, 셀·타일 구조 |
| `text` | 원문 전사와 확정 역문 |
| `style` | 획·색·외곽선·그림자·셀·행 메트릭 |
| `priority` | 노출 빈도와 중요도에 따른 작업 순서 |
| `state` | 조사 전, 텍스트 없음, 불명, 발견, 복원, 조판, 검증의 현재 단계 |
| `evidence` | 크롭·스크린샷·마스크·클린 플레이트 해시·인게임 검증 포인터 |

상태명은 프로젝트 언어에 맞춰 바꿀 수 있지만 “조사하지 않음”, “조사했으나 없음”, “불명확”을 서로 다른 값으로 둔다. 바운딩 박스는 복원·조판 입력이자 영역 밖 픽셀 검증 마스크이므로 한 필드에서만 관리한다. 좌표가 백분율로 들어오면 원본 이미지 크기와 함께 저장하거나 픽셀로 정규화한다.

## 6. HITL 관측 요청

한 요청은 사람이 한 번의 관측 세션으로 답할 수 있는 범위로 자른다. 다음 표는 필요한 의미와 선택 가능한 필드명을 보여 준다.

| 필드 | 내용 |
|---|---|
| `request_id` | 안정 ID |
| `baseline` | ROM·패치·에뮬레이터·설정 식별자 |
| `setup` | 시작 상태와 짧은 입력·재현 절차 |
| `trigger` | 브레이크포인트·화면·프레임 등 관측 시점 |
| `capture` | 읽을 레지스터·메모리·트레이스·스크린샷 |
| `branches` | 관측 결과별 판정과 다음 행동 |
| `response` | 실제 값, 캡처 포인터, 관측자 메모 |
| `outcome` | 확정, 기각, 무관, 불명확 중 판정 |

요청 인덱스에는 ID, 대상 가설, 현재 판정, 응답 위치만 둔다. 본문을 인덱스에 복제하지 않는다.

## 7. QA 라운드와 이슈

### 7.1 QA 라운드

라운드 기록은 기준 빌드와 검증 범위를 고정하고 이슈·커버리지·종료 판정을 묶는다. 빌드 식별자, 대상 환경과 범위, 자동 게이트 결과, 필요한 경로의 커버리지, 연결된 이슈, 종료 판정과 남은 블로커를 구분한다.

각 텍스트 표시 영역의 레이아웃 제약은 영역 식별자, 렌더러·박스 범위, 폭 계산 규칙, 줄·페이지 한도, 줄바꿈·페이지 제어의 상태 영향, 위반 심각도를 구분한다. 구체 수치는 대상 게임에서 실측한다.

### 7.2 개별 이슈

다음 표는 필요한 의미와 선택 가능한 필드명을 보여 준다.

| 필드 | 내용 |
|---|---|
| `issue_id` | 라운드를 넘어도 유지되는 안정 ID |
| `summary` | 관찰된 증상 중심의 짧은 제목 |
| `baseline` | 재현한 빌드와 환경 |
| `reproduction` | 직전 상태, 짧은 입력, 기대 결과와 실제 결과 |
| `evidence` | 스크린샷·덤프·트레이스 포인터 |
| `hypotheses` | 미확정 원인과 각 판정 실험 |
| `rejected` | 기각한 가설과 기각 증거 |
| `mechanism` | 격리로 확정한 결함→감염→실패 사슬 |
| `change` | 확정 결함에 적용한 수정과 영향 범위 |
| `regression` | 수정 전 실패·수정 후 통과 증거와 회귀 편입 위치 |
| `state` | 열림, 조사 중, 재확인 필요, 수정 종료, 버그 아님, 범위 밖 |

증거 파일의 이름이나 저장 위치가 이슈의 제목·상태를 대신하지 않게 한다. 첫 보고는 최소한 기준선, 재현 맥락, 기대 결과와 실제 결과, 판정에 필요한 증거를 구분한다. 재사용 가능한 시작 상태가 재현 비용을 실질적으로 줄일 때만 함께 연결한다.

## 8. 기록 검증

기계 판독 기록은 빌드나 전용 검사에서 다음을 확인한다.

- 필수 ID가 비어 있거나 중복되지 않는다.
- 참조한 프로젝트 상대 경로와 항목 ID가 존재한다.
- 가설과 확정 결론이 같은 필드에 섞이지 않는다.
- 통과·실패·불명확 판정에는 근거와 다음 행동이 있다.
- 종료된 이슈에는 재현 기준선과 종료 증거가 있다.
- 카탈로그의 완료 분자와 조사 대상 분모가 일치하며 불명 항목이 숨겨지지 않는다.

Markdown만 쓰는 프로젝트에서도 이 의미를 표와 링크로 표현할 수 있다. JSON·YAML·데이터베이스를 쓰면 스키마 검사를 자동화하되, 직렬화 형식 자체를 전략 판정으로 취급하지 않는다.
